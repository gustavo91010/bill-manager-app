FROM node:22-alpine

WORKDIR /app

# Copia apenas arquivos de dependências primeiro para aproveitar o cache de build
COPY package.json pnpm-lock.yaml* ./

RUN npm install -g pnpm && pnpm install

# Copia o restante do código
COPY . .

EXPOSE 3000

CMD ["pnpm", "dev"]